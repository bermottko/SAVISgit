<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Savis</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=League+Spartan:wght@100..900&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
        }
        .body-cadastro {
            font-family: Arial, sans-serif;
            background-color: #00204c;
        }
        .jumbotron {
            background-color: #007bff;
            color: white;
            padding: 2rem 1rem;
            margin-bottom: 2rem;
            border-radius: 0.5rem;
        }
        .jumbotron h1 {
            font-size: 2.5rem;
            font-weight: bold;
        }
        .btn-custom {
            background-color: #28a745;
            color: white;
            border-radius: 25px;
            padding: 0.75rem 1.5rem;
            font-size: 1.1rem;
            margin-top: 1rem;
            transition: background-color 0.3s ease;
        }
        .btn-custom:hover {
            background-color: #218838;
        }
        .feature-card {
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-bottom: 1.5rem;
            padding: 1.5rem;
            background-color: white;
            text-align: center;
        }
        .feature-card h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }
        .navbar-brand {
            font-size: 1.5rem;
            font-weight: bold;
        }
    /*TELA INCIAL*/
        .container-tela-inicial {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding-bottom: 130px; /* empurra pra baixo, fazendo parecer mais alto */
            box-sizing: border-box;
        }
        .logo-img-nome {
            display: flex;
            flex-direction: row;
            align-items: center;
            justify-content: center;
            gap: 16px;
            padding-right: 80px;
        }
        .logo-img{
            width: 150px;
        }
        .logo-nome {
            font-family: 'League Spartan', sans-serif;
            font-size: 130px;
            color: #00204c;
            font-weight: bold;
            margin-top: 140px;
        }
        .logo-nome-cadastro {
            font-family: 'League Spartan', sans-serif;
            color: #235284;
            font-weight: bold;
            font-size: 22px;
        }
        .logo-subnome {
            font-family: 'League Spartan', sans-serif;
            font-size: 20px; /* menor que o nome */
            color: #235284; /* tom azul mais claro ou o que preferir */
            margin-top: 6px; /* espaço superior para afastar da logo-nome */
            text-align: center; /* centraliza o texto */
            font-weight: bold;
}
        .botoes{
            display: flex;
            gap: 40px; /* Espaçamento horizontal entre os botões */
            justify-content: center; /* Centraliza os botões */
            margin-top: 65px;
        }

        .botao {
            display: inline-block;
            background-color: #00204c; /* azul básico */
            color: white; /* texto branco */
            padding: 10px 20px; /* espaço interno */
            border-radius: 5px; /* cantos levemente arredondados */
            text-decoration: none; /* remove sublinhado */
        }
        .botao:hover {
            background-color: #001129; /* azul um pouco mais escuro no hover */
        }
    /*CADASTRO*/
        .botao-cadastro{
            background-color: #00204c;
            color: white;
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            margin-top: 30px; 
            text-align: center; /* centraliza o texto dentro do botão */
            width: fit-content; /* botão só do tamanho do conteúdo */
            box-shadow: none; /* remove sombra */
            border: none; /* remove borda */
        }
        .botao-cadastro:hover {
            background-color: #001129; /* azul um pouco mais escuro no hover */
        }
        .criar-conta {
            text-decoration: none;
        }
        .texto-criar-conta {
            font-size: 14px;
            display: block; /* Garante que a margem funcione */
            margin-top: 30px; /* Ajuste conforme o espaço desejado */
            text-align: center;
        }

    </style>
</head>
<body>
    <%- body %>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
